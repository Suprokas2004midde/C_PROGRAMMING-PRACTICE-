#include<stdio.h>
int main ()
{
    int x=10,y=15;
    if ( x%2 == y%3)
    /*here right side executed value would be assign to the x%2 value.
    so, the there will be an error in x to the condition..... */
    printf("carpathians\n");
    return 0;
}